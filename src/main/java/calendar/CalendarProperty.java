package calendar;

/**
 * Editable properties of a calendar.
 */
public enum CalendarProperty {
  /**
   * The unique display name of a calendar.
   */
  NAME,

  /**
   * The calendar's IANA timezone identifier (e.g., "America/New_York").
   */
  TIMEZONE
}
